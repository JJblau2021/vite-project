import React from "react";
import { Layout } from "@douyinfe/semi-ui";
import "./index.less";

const Header = ({ ...props }) => {
  return <Layout.Header {...props}>Header</Layout.Header>;
};

export default Header;
